# Optional when the frontend is served behind a reverse proxy exposing /api.
# For a separate backend, set this to the reachable backend origin, for example:
# VITE_API_BASE_URL=http://192.168.1.50:9999
VITE_API_BASE_URL=
